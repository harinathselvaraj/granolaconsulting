# Dependencies & build tooling
.venv/
.tools/
node_modules/

# Vercel CLI local link
.vercel

# OS / editor
.DS_Store
.cursor/
.claude/

# HoneyGold backend — tracked in its own separate git repository (repo root only)
/honeygold/

# Local backend / tooling not part of the Vercel-deployed sites
api/
scripts/

# HoneyGold backend has its own git repo (see docs/project-structure.md)
# /honeygold/ is ignored at monorepo root — commit backend changes in honeygold/.git
